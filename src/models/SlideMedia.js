export default class SlideMedia {
	constructor(url, caption = null, credit = null, thumbnail = null) {
		this.url = url
		this.caption = caption
		this.credit = credit
		this.thumbnail = thumbnail
	}
}
